# 家常三菜一汤 - 本地部署指南

这是一个基于 AI 的家庭食谱生成应用。您可以按照以下步骤在自己的电脑上运行它，并分享给家人使用。

## 1. 环境准备

在开始之前，请确保您的电脑已安装：
- **Node.js** (建议版本 18.0 或更高): [下载地址](https://nodejs.org/)
- **Git** (可选，用于克隆代码)

## 2. 获取代码

1. 在您的电脑上创建一个新文件夹。
2. 将本项目的所有文件下载或复制到该文件夹中。

## 3. 配置 API 密钥

由于应用使用了 Google Gemini AI，您需要配置自己的密钥：

1. 访问 [Google AI Studio](https://aistudio.google.com/)。
2. 点击 "Get API key" 并创建一个新的 API Key。
3. 在项目根目录下创建一个名为 `.env` 的文件。
4. 在 `.env` 文件中添加以下内容：
   ```env
   GEMINI_API_KEY=您的_API_KEY_粘贴在这里
   ```

## 4. 安装与启动

打开终端（Command Prompt, PowerShell 或 Terminal），进入项目目录：

1. **安装依赖**：
   ```bash
   npm install
   ```

2. **启动应用**：
   ```bash
   npm run dev
   ```

3. **访问应用**：
   在浏览器中打开终端显示的地址（通常是 `http://localhost:5173`）。

## 5. 分享给家人（局域网访问）

如果您想让家人在手机或其他设备上访问：

1. 确保所有设备连接在同一个 Wi-Fi 下。
2. 使用以下命令启动：
   ```bash
   npm run dev -- --host
   ```
3. 终端会显示一个 `Network` 地址（例如 `http://192.168.1.5:5173`）。
4. 让家人在手机浏览器中输入这个地址即可。

## 6. 常见问题

- **图片无法显示**：请检查您的网络是否可以正常访问 Google 服务（Gemini 图片生成需要网络支持）。
- **安装失败**：请确保 Node.js 版本正确，并尝试删除 `node_modules` 文件夹后重新运行 `npm install`。
